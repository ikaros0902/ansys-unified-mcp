---
name: <技能名稱>
description: <一句話描述此技能的用途，供漸進式披露時預設載入>
---

# Skill_Document 樣板

> 用途：以文件形式編碼專案知識與慣例，供 AI 代理在 Loop 執行時依漸進式披露原則載入。
> 建議存放位置：專案內 skills 目錄（例如 `skills/<skill-name>/SKILL.md`）。
> 填寫說明：`name` 與 `description` front matter 為必填，預設僅載入此兩者；`適用條件` 成立時才載入以下詳細章節。將 `<...>` 佔位符替換為實際內容即可，勿寫入任何特定工具的專屬語法或指令。

## metadata

| 欄位 | 說明 | 填寫內容 |
| --- | --- | --- |
| 名稱 | 技能識別名，須與 front matter 的 `name` 一致 | `<技能名稱>` |
| 描述 | 一句話描述用途，須與 front matter 的 `description` 一致 | `<一句話描述>` |
| 適用條件 | 何種情境成立時才載入以下詳細內容 | `<例如：修改資料存取層時 / 撰寫 API 端點時>` |

## 適用場景

`<描述此技能適用的具體任務或情境，讓 AI 代理判斷是否需要載入詳細內容。>`

- `<場景一>`
- `<場景二>`

## 專案慣例

`<記錄此領域的專案慣例、命名規則、目錄結構、必守的設計約束等。>`

- `<慣例一>`
- `<慣例二>`

## 常見陷阱

`<記錄此領域容易出錯的地方與對應的正確做法，協助 AI 代理避開已知問題。>`

- `<陷阱一>`：`<避免方式>`
- `<陷阱二>`：`<避免方式>`

## 填寫檢查

- [ ] front matter 已填 `name` 與 `description` 兩個必填欄位。
- [ ] metadata 的名稱、描述與 front matter 一致，並填妥適用條件。
- [ ] 適用場景、專案慣例、常見陷阱三章節皆已填寫。
- [ ] 全文以抽象語言撰寫，未寫入任何工具專屬的指令或語法。
