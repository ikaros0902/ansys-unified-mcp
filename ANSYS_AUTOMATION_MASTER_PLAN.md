# ANSYS MCP 全自動化開發藍圖與多模型動態路由架構計畫

## 一、目前您擁有的全部資源盤點 (Resource Inventory)

在您的電腦中，您已經具備了打造一套頂級「AI 驅動 CAE 自動化系統」所需的所有基建：

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│                                 您的核心資產全景圖                                │
├──────────────────────────────────────────────────────────────────────────────────┤
│ 1. 商業 CAE 核心底座 (ANSYS 2025 R1 安裝於 C:\Program Files\ANSYS Inc\v251)      │
│    ├─ SpaceClaim (SCDM) 幾何引擎 (gRPC Port 50051 + IronPython Scripting)        │
│    ├─ Mechanical 結構/熱力求解器 (ACT API + MAPDL APDL Commands Snippets)        │
│    ├─ Fluent 流體動力學求解器 (PyFluent + Scheme/TUI + UDF 編譯器)                │
│    ├─ Workbench 專案管理 (PyWorkbench + FileBridge + Plugin Queue)                │
│    └─ optiSLang 參數最佳化與靈敏度分析 (PyOptiSLang)                             │
├──────────────────────────────────────────────────────────────────────────────────┤
│ 2. 統一中繼伺服器 (F:\Ming_python\ansys-unified-mcp)                             │
│    ├─ 40+ 個 FastMCP 原生工具 (覆蓋 geometry_*, mechanical_*, fluent_*, optislang)│
│    ├─ 本地 SQLite FTS5 全文檢索 RAG (Documentation_clean\docs_index.sqlite)       │
│    └─ 10 項純淨 ANSYS CAE 專業技能 (SKILLs/ 目錄)                                │
├──────────────────────────────────────────────────────────────────────────────────┤
│ 3. 雙 Agent 執行環境                                                            │
│    ├─ Antigravity (IDE 級深度開發 Agent，掛載 Serena 代碼導航與 CodeGraph)        │
│    └─ Hermes Agent (F:\Hermes，支援 CLI 互動 + Web Dashboard 儀表板控制)         │
├──────────────────────────────────────────────────────────────────────────────────┤
│ 4. 本機算力資源 (AMD Ryzen 9 5900X 24緒 / 64GB RAM / RTX 4060 Ti 8GB VRAM)      │
│    └─ Ollama 端點 (http://127.0.0.1:11434/v1)                                    │
└──────────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、您的電腦能負荷哪個模型？（硬體負載矩陣）

您的硬體關鍵特性在於 **8 GB VRAM**，但擁有 **64 GB 的超大系統記憶體與 12 核 24 緒的頂級 CPU**：

| 模型規模 | 代表模型 | 記憶體佔用 | 推理速度 (tok/s) | 系統負載狀態 | 適用角色 |
| :--- | :--- | :---: | :---: | :---: | :--- |
| **7B ~ 9B** | `Qwen 3.5 9B`<br>`Qwen 3:8B`<br>`Phi-4-mini` | 4.8 ~ 6.2 GB | **40 ~ 60 tok/s**<br>(極速流暢) | 🟢 **100% 顯存 (VRAM)**<br>完全不掉速 | **高頻調度器 / 路由器**<br>MCP 工具呼叫、狀態查詢、指令分流 |
| **14B** | `DeepSeek R1 14B`<br>`Qwen 2.5 Coder 14B`<br>`Qwen 3.5 14B` | 8.8 ~ 10.5 GB | **4 ~ 8 tok/s**<br>(略有頓感但可用) | 🟡 **GPU+CPU 混合分流**<br>(6GB 顯存 + 4GB 記憶體) | **CAE 專家 / 腳本工程師**<br>寫 APDL、SpaceClaim ACT、ROM 算式 |
| **31B+** | `Gemma 4 31B`<br>`Qwen 3.6 23GB` | 19 ~ 25 GB | **1 ~ 3 tok/s**<br>(卡頓明顯) | 🔴 **大量依賴 CPU 記憶體**<br>PCIe 頻寬卡死 | ❌ **不建議作為互動式 Agent** |

---

## 三、仿 Claude 的「多模型動態分工路由架構」（Model Routing）

您可以完全像 Claude / 商業級 Agent 一樣，依據任務複雜度自動將工作分派給最適模型：

```
                              【使用者自然語言輸入】
                                        │
                                        ▼
             ┌─────────────────────────────────────────────────────┐
             │       Level 1: 輕量快速路由器 (Router Agent)         │
             │           模型：Qwen 3.5 9B (完全 GPU, ~50 tok/s)    │
             │   職責：意圖識別、關鍵字分類、MCP 工具一階呼叫      │
             └──────────────────────────┬──────────────────────────┘
                                        │
                 ┌──────────────────────┴──────────────────────┐
                 ▼ (簡單操作/狀態查詢)                         ▼ (複雜幾何/有限元力學分析)
   ┌───────────────────────────┐                 ┌───────────────────────────┐
   │ Level 2A: 即時執行器       │                 │ Level 2B: CAE 推理專家     │
   │ 模型：Qwen 3.5 9B          │                 │ 模型：DeepSeek R1 14B     │
   │ 職責：                    │                 │ 職責：                    │
   │ • 呼叫 geometry_create_*  │                 │ • 79層 PCB 疊構自動化規劃 │
   │ • 查詢 Workbench 專案狀態 │                 │ • 生成 APDL 複合材料巨集  │
   │ • 啟動/停止 ANSYS 例項    │                 │ • 3-2-1 靜定支承防過約束   │
   └─────────────┬─────────────┘                 └─────────────┬─────────────┘
                 │                                             │
                 └──────────────────────┬──────────────────────┘
                                        ▼
             ┌─────────────────────────────────────────────────────┐
             │       Level 3: 驗證與自我修復閉環 (Verifier Agent)    │
             │       模型：Qwen 3.5 9B + ansys-error-catalog 知識庫 │
             │ 職責：解析 Mechanical/Fluent 解算 Log、自動修正語法錯誤│
             └─────────────────────────────────────────────────────┘
```

---

## 四、ANSYS 全自動化五階段落地詳細計畫

### 階段 1：基建與雙模型路由配置（第 1~2 天）
1. **本地模型就位**：
   * 下載 `Qwen 3.5 9B` 作為主力高頻調度模型（顯存內 100% 運行）。
   * 保留 `DeepSeek R1 14B` 作為高難度力學與 APDL 專用推理模型。
2. **在 Hermes Agent / Antigravity 配置模型別名與分流**：
   * 建立 Router 模式，使高頻對話與幾何指令由 9B 快速回應；複雜分析呼叫 R1 14B 子代理。

### 階段 2：幾何與網格全自動閉環（第 3~5 天）
1. **SpaceClaim 雙軌驅動強化**：
   * 將 `ansys-spaceclaim` 與 `geometry_*` MCP 工具連結，實現「一鍵啟動 + 自動換算公尺 SI 單位 + 無頭/有介面建模」。
2. **MultiZone / 網格自動劃分與評估**：
   * 封裝 Mechanical 網格自動化腳本，自動檢查 Jacobian 歪斜度與網格品質，不合格自動細化。

### 階段 3：材料庫與 APDL 物理載荷管線（第 6~8 天）
1. **ROM 複合材料矩陣生成器**：
   * 串聯 Excel 疊構讀取 ➜ 自動計算 23 點溫度相關等效力學常數 ($E, \text{CTE}, \nu$) ➜ 輸出符合 MAPDL 嚴格順序規範的 `.mac` 巨集。
2. **標準力學邊界範本庫**：
   * 固化 3-2-1 靜定支承、熱對流邊界、對稱面邊界等防過約束範本。

### 階段 4：求解監控、錯誤自愈與報告生成（第 9~11 天）
1. **求解器狀態即時監聽**：
   * 利用 `get_solve_status` 監聽 MAPDL / Fluent 殘差收斂曲線。
2. **錯誤分類自愈（Self-Healing Loop）**：
   * 當求解器報錯（如 `Pivot error`, `Shape testing failed`），自動檢索 `ansys-error-catalog`，由 R1 14B 分析原因並直接在 Mechanical 修正接觸或網格設定後自動重算。
3. **成果產出**：
   * 自動截取 Mechanical 變形雲圖（Z-displacement warpage）與等效应力雲圖，彙整為 Markdown / PDF 報告。

### 階段 5：端到端「一句話 CAE」驗收（第 12~14 天）
* **終極目標測試**：
  使用者輸入：
  > *「請讀取 MCP_Test.xlsx，在 SpaceClaim 建立 79 層 PCB 幾何並設定 Share Topology，套用 30°C 至 220°C 熱載荷與 3-2-1 支承，完成求解並產出翹曲分析報告。」*
* 系統完全自主調用各層工具，無需人工干預，耗時 < 5 分鐘全自動產出結果。
